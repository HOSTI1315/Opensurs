local player = game.Players.LocalPlayer

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local RunService = game:GetService("RunService")

local UserInputService = game:GetService("UserInputService")

local TweenService = game:GetService("TweenService")



-- ===== Vars =====

local enabled = true -- otomatis on

local currentSpeed = 150

local flySpeed = 50

local DEFAULT_SPEED = 16

local character, humanoid, hrp

local speedConnection

local fakeFireLoop

local itemID = "Grapple Hook"

local useItemRE = ReplicatedStorage:WaitForChild("Packages")

    :WaitForChild("Net")

    :WaitForChild("RE/UseItem")



-- Fly variables

local flying = false

local flyConnection

local flyUp = false

local flyDown = false



-- Mobile detection

local isMobile = UserInputService.TouchEnabled



-- ===== GUI =====

local gui = Instance.new("ScreenGui")

gui.Name = "WalvyCombinedGUI"

gui.ResetOnSpawn = false

gui.Parent = player:WaitForChild("PlayerGui")



local frame = Instance.new("Frame", gui)

frame.Size = isMobile and UDim2.new(0, 250, 0, 280) or UDim2.new(0, 220, 0, 200)

frame.Position = UDim2.new(0.5, isMobile and -125 or -110, 0.5, isMobile and -140 or -100)

frame.BackgroundColor3 = Color3.fromRGB(25,25,25)

frame.BorderSizePixel = 0

frame.AnchorPoint = Vector2.new(0.5,0.5)

frame.Active = true

Instance.new("UICorner", frame).CornerRadius = UDim.new(0,8)



local title = Instance.new("TextLabel", frame)

title.Text = "raped by porxiai | yes daddy 😩☹️"

title.Size = UDim2.new(1,0,0,25)

title.Position = UDim2.new(0,0,0,5)

title.BackgroundTransparency = 1

title.TextColor3 = Color3.fromRGB(255,255,255)

title.Font = Enum.Font.GothamBold

title.TextSize = 14



-- Master Toggle Button

local toggleButton = Instance.new("TextButton", frame)

toggleButton.Size = UDim2.new(0.8,0,0,30)

toggleButton.Position = UDim2.new(0.1,0,0.15,0)

toggleButton.BackgroundColor3 = Color3.fromRGB(40,40,40)

toggleButton.TextColor3 = Color3.fromRGB(255,255,255)

toggleButton.Font = Enum.Font.GothamBold

toggleButton.TextSize = 12

toggleButton.Text = enabled and "⏸ Turn Off Fly" or "▶ Turn On Fly"

Instance.new("UICorner", toggleButton).CornerRadius = UDim.new(0,6)



-- Fly Toggle Button

local flyToggleButton = Instance.new("TextButton", frame)

flyToggleButton.Size = UDim2.new(0.8,0,0,30)

flyToggleButton.Position = UDim2.new(0.1,0,0.3,0)

flyToggleButton.BackgroundColor3 = Color3.fromRGB(60,60,180)

flyToggleButton.TextColor3 = Color3.fromRGB(255,255,255)

flyToggleButton.Font = Enum.Font.GothamBold

flyToggleButton.TextSize = 12

flyToggleButton.Text = "🔄 Toggle Fly"

Instance.new("UICorner", flyToggleButton).CornerRadius = UDim.new(0,6)



-- Speed Input

local speedInput = Instance.new("TextBox", frame)

speedInput.Size = UDim2.new(0.8,0,0,25)

speedInput.Position = UDim2.new(0.1,0,0.45,0)

speedInput.PlaceholderText = "Fly Speed (0-250)"

speedInput.ClearTextOnFocus = false

speedInput.Text = tostring(flySpeed)

speedInput.BackgroundColor3 = Color3.fromRGB(35,35,35)

speedInput.TextColor3 = Color3.fromRGB(255,255,255)

speedInput.Font = Enum.Font.Gotham

speedInput.TextSize = 11

Instance.new("UICorner", speedInput).CornerRadius = UDim.new(0,5)



-- Controls Info

local controlsLabel = Instance.new("TextLabel", frame)

controlsLabel.Text = isMobile and "Controls: Buttons Below" or "Controls: E=Up, Q=Down"

controlsLabel.Size = UDim2.new(0.8,0,0,20)

controlsLabel.Position = UDim2.new(0.1,0,0.6,0)

controlsLabel.BackgroundTransparency = 1

controlsLabel.TextColor3 = Color3.fromRGB(200,200,200)

controlsLabel.Font = Enum.Font.Gotham

controlsLabel.TextSize = 10



-- Status Label

local statusLabel = Instance.new("TextLabel", frame)

statusLabel.Text = "Status: Flying OFF"

statusLabel.Size = UDim2.new(0.8,0,0,20)

statusLabel.Position = UDim2.new(0.1,0,0.7,0)

statusLabel.BackgroundTransparency = 1

statusLabel.TextColor3 = Color3.fromRGB(0,255,0)

statusLabel.Font = Enum.Font.Gotham

statusLabel.TextSize = 10



-- Mobile Controls (only show on mobile)

local mobileControls

local upButton, downButton



if isMobile then

    mobileControls = Instance.new("Frame", gui)

    mobileControls.Size = UDim2.new(0, 150, 0, 120)

    mobileControls.Position = UDim2.new(1, -160, 0.5, -60)

    mobileControls.BackgroundColor3 = Color3.fromRGB(25,25,25)

    mobileControls.BackgroundTransparency = 0.3

    mobileControls.BorderSizePixel = 0

    Instance.new("UICorner", mobileControls).CornerRadius = UDim.new(0,8)

    

    local controlsTitle = Instance.new("TextLabel", mobileControls)

    controlsTitle.Text = "Fly Controls"

    controlsTitle.Size = UDim2.new(1,0,0,25)

    controlsTitle.Position = UDim2.new(0,0,0,5)

    controlsTitle.BackgroundTransparency = 1

    controlsTitle.TextColor3 = Color3.fromRGB(255,255,255)

    controlsTitle.Font = Enum.Font.GothamBold

    controlsTitle.TextSize = 14

    

    -- Up Button

    upButton = Instance.new("TextButton", mobileControls)

    upButton.Text = "🔼 UP"

    upButton.Size = UDim2.new(0.8,0,0,40)

    upButton.Position = UDim2.new(0.1,0,0.3,0)

    upButton.BackgroundColor3 = Color3.fromRGB(0,150,0)

    upButton.TextColor3 = Color3.fromRGB(255,255,255)

    upButton.Font = Enum.Font.GothamBold

    upButton.TextSize = 14

    upButton.AutoButtonColor = false

    Instance.new("UICorner", upButton).CornerRadius = UDim.new(0,6)

    

    -- Down Button

    downButton = Instance.new("TextButton", mobileControls)

    downButton.Text = "🔽 DOWN"

    downButton.Size = UDim2.new(0.8,0,0,40)

    downButton.Position = UDim2.new(0.1,0,0.7,0)

    downButton.BackgroundColor3 = Color3.fromRGB(150,0,0)

    downButton.TextColor3 = Color3.fromRGB(255,255,255)

    downButton.Font = Enum.Font.GothamBold

    downButton.TextSize = 14

    downButton.AutoButtonColor = false

    Instance.new("UICorner", downButton).CornerRadius = UDim.new(0,6)

end



-- ===== Functions =====

local function updateCharacter()

    character = player.Character or player.CharacterAdded:Wait()

    humanoid = character and character:WaitForChild("Humanoid", 5)

    hrp = character and character:WaitForChild("HumanoidRootPart", 5)

    if humanoid then

        humanoid.JumpPower = 50

    end

end



-- Fly System

local function startFlying()

    if flyConnection then flyConnection:Disconnect() flyConnection = nil end

    if not flying or not hrp then return end

    

    flyConnection = RunService.Heartbeat:Connect(function()

        if not flying or not hrp then return end

        

        local moveDirection = Vector3.new(0, 0, 0)

        

        -- Get movement direction from humanoid

        if humanoid then

            moveDirection = humanoid.MoveDirection

        end

        

        -- Add up/down movement

        if flyUp then

            moveDirection = moveDirection + Vector3.new(0, 1, 0)

        elseif flyDown then

            moveDirection = moveDirection + Vector3.new(0, -1, 0)

        end

        

        -- Normalize and apply speed

        if moveDirection.Magnitude > 0 then

            moveDirection = moveDirection.Unit * flySpeed

        end

        

        -- Apply velocity for smooth flying

        hrp.AssemblyLinearVelocity = Vector3.new(moveDirection.X, moveDirection.Y, moveDirection.Z)

        

        -- Anti-gravity (prevent falling)

        hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)

    end)

end



local function stopFlying()

    if flyConnection then

        flyConnection:Disconnect()

        flyConnection = nil

    end

    if hrp then

        hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)

    end

end



-- Mobile button animation

local function animateButton(button, pressed)

    local tweenInfo = TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    local goal = {}

    

    if pressed then

        goal.BackgroundColor3 = button == upButton and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(200, 0, 0)

        goal.Size = UDim2.new(0.75, 0, 0.35, 0)

    else

        goal.BackgroundColor3 = button == upButton and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(150, 0, 0)

        goal.Size = UDim2.new(0.8, 0, 0.4, 0)

    end

    

    local tween = TweenService:Create(button, tweenInfo, goal)

    tween:Play()

end



-- Grapple Hook (kept for compatibility)

local function buyGrapple()

    pcall(function()

        ReplicatedStorage:WaitForChild("Packages")

            :WaitForChild("Net")

            :WaitForChild("RF/CoinsShopService/RequestBuy")

            :InvokeServer(itemID)

    end)

end



local function autoEquip()

    pcall(function()

        local character = player.Character

        local humanoid = character and character:FindFirstChildOfClass("Humanoid")

        if not (character and humanoid and humanoid.Health>0) then return end

        local currentTool = character:FindFirstChildOfClass("Tool")

        if currentTool and currentTool.Name==itemID then return end

        local backpack = player:WaitForChild("Backpack")

        local grappleInBackpack = backpack:FindFirstChild(itemID)

        if grappleInBackpack then humanoid:EquipTool(grappleInBackpack) end

    end)

end



local function fakeFire()

    autoEquip()

    local args = {1.9832406361897787}

    pcall(function()

        useItemRE:FireServer(unpack(args))

    end)

end



local function startFakeFireLoop()

    if fakeFireLoop then fakeFireLoop:Disconnect() end

    fakeFireLoop = RunService.Heartbeat:Connect(function()

        if enabled then fakeFire() end

    end)

end



local function stopAll()

    stopFlying()

    if fakeFireLoop then fakeFireLoop:Disconnect() fakeFireLoop=nil end

    flying = false

    flyUp = false

    flyDown = false

    statusLabel.Text = "Status: Flying OFF"

    statusLabel.TextColor3 = Color3.fromRGB(255, 0, 0)

    

    -- Reset mobile buttons

    if isMobile then

        animateButton(upButton, false)

        animateButton(downButton, false)

    end

end



-- ===== Input Handling =====

if not isMobile then

    -- PC Controls

    UserInputService.InputBegan:Connect(function(input, gameProcessed)

        if gameProcessed then return end

        

        if input.KeyCode == Enum.KeyCode.E then

            flyUp = true

        elseif input.KeyCode == Enum.KeyCode.Q then

            flyDown = true

        end

    end)



    UserInputService.InputEnded:Connect(function(input, gameProcessed)

        if input.KeyCode == Enum.KeyCode.E then

            flyUp = false

        elseif input.KeyCode == Enum.KeyCode.Q then

            flyDown = false

        end

    end)

else

    -- Mobile Controls

    upButton.MouseButton1Down:Connect(function()

        flyUp = true

        animateButton(upButton, true)

    end)

    

    upButton.MouseButton1Up:Connect(function()

        flyUp = false

        animateButton(upButton, false)

    end)

    

    upButton.MouseLeave:Connect(function()

        flyUp = false

        animateButton(upButton, false)

    end)

    

    downButton.MouseButton1Down:Connect(function()

        flyDown = true

        animateButton(downButton, true)

    end)

    

    downButton.MouseButton1Up:Connect(function()

        flyDown = false

        animateButton(downButton, false)

    end)

    

    downButton.MouseLeave:Connect(function()

        flyDown = false

        animateButton(downButton, false)

    end)

end



-- ===== Events =====

toggleButton.MouseButton1Click:Connect(function()

    enabled = not enabled

    updateCharacter()

    if enabled then

        toggleButton.Text = "⏸ Turn Off Fly"

        buyGrapple()

        startFakeFireLoop()

        statusLabel.Text = "Status: Ready (Toggle Fly)"

        statusLabel.TextColor3 = Color3.fromRGB(255, 255, 0)

    else

        toggleButton.Text = "▶ Turn ON Fly"

        stopAll()

    end

end)



flyToggleButton.MouseButton1Click:Connect(function()

    if not enabled then return end

    

    flying = not flying

    updateCharacter()

    

    if flying then

        startFlying()

        statusLabel.Text = isMobile and "Status: Flying ON (Use Buttons)" or "Status: Flying ON (E=Up, Q=Down)"

        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)

        flyToggleButton.BackgroundColor3 = Color3.fromRGB(0, 180, 0)

    else

        stopFlying()

        statusLabel.Text = "Status: Flying OFF"

        statusLabel.TextColor3 = Color3.fromRGB(255, 0, 0)

        flyToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 180)

    end

end)



speedInput:GetPropertyChangedSignal("Text"):Connect(function()

    local val = tonumber(speedInput.Text)

    if val then

        flySpeed = math.clamp(val, 10, 500)

        speedInput.Text = tostring(flySpeed)

    end

end)



-- ===== Drag GUI =====

local dragging, dragInput, dragStart, startPos

frame.InputBegan:Connect(function(input)

    if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then

        dragging=true

        dragStart=input.Position

        startPos=frame.Position

        input.Changed:Connect(function()

            if input.UserInputState==Enum.UserInputState.End then dragging=false end

        end)

    end

end)

frame.InputChanged:Connect(function(input)

    if input.UserInputType==Enum.UserInputType.MouseMovement or input.UserInputType==Enum.UserInputType.Touch then

        dragInput=input

    end

end)

UserInputService.InputChanged:Connect(function(input)

    if dragging and input==dragInput then

        local delta=input.Position - dragStart

        frame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset+delta.X,

                                 startPos.Y.Scale,startPos.Y.Offset+delta.Y)

    end

end)



-- Drag mobile controls too

if isMobile then

    mobileControls.InputBegan:Connect(function(input)

        if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then

            dragging=true

            dragStart=input.Position

            startPos=mobileControls.Position

            input.Changed:Connect(function()

                if input.UserInputState==Enum.UserInputState.End then dragging=false end

            end)

        end

    end)

    mobileControls.InputChanged:Connect(function(input)

        if input.UserInputType==Enum.UserInputType.MouseMovement or input.UserInputType==Enum.UserInputType.Touch then

            dragInput=input

        end

    end)

end



-- ===== Initial Setup =====

updateCharacter()

buyGrapple()

startFakeFireLoop()



-- ===== Respawn Handling =====

player.CharacterAdded:Connect(function()

    task.wait(0.5)

    if enabled then

        updateCharacter()

        if flying then

            startFlying()

        end

        startFakeFireLoop()

    end

end)